#include <stdio.h>
#include <stdlib.h>

int main(void) {
  return 0;
}

// example: fibbonacci
int fib(int n) {

}

// example: factorial - function calls itself
int factorial(int n) {
  if (n >= 1) return n * factorial(n - 1);
  else return 1;
}
